steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'docker/Dockerfile.api'
      - '-t'
      - 'gcr.io/$PROJECT_ID/plataforma-api'
      - '.'
images:
  - 'gcr.io/$PROJECT_ID/plataforma-api'
timeout: '600s'
